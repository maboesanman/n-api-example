{
  "name": "n-api-example",
  "version": "1.0.0",
  "description": "example implementation of an n-api based node addon",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "start": "NODE_ENV=release node index.js",
    "debug": "NODE_ENV=debug node --nolazy --inspect-brk index.js",
    "build": "node-gyp build -j max ",
    "build:debug": "node-gyp build -j max --debug",
    "rebuild": "node-gyp rebuild -j max",
    "rebuild:debug": "node-gyp rebuild -j max --debug",
    "clean": "node-gyp clean"
  },
  "author": "Mason Boeman",
  "license": "ISC",
  "devDependencies": {
    "node-gyp": "^6.0.0"
  },
  "dependencies": {
    "node-addon-api": "^1.7.1"
  }
}
